// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::gen::runtime::{self, CliOverrides, Context};

pub fn command() -> Command {
    Command::new("create")
        .subcommand_required(true)
        .arg_required_else_help(true)
        .subcommand(
            Command::new("api")
                .subcommand_negates_reqs(true)
                .arg(
                    Arg::new("name")
                        .long("name")
                        .required(true),
                )
                .arg(
                    Arg::new("id")
                        .long("id")
                        .help("Explicit id/slug for the entry. Slugified server-side; defaults to a slug\nof `name`. Lets the display title (`name`) be decoupled from the stable id."),
                )
                .arg(
                    Arg::new("format")
                        .long("format"),
                )
                .arg(
                    Arg::new("kind")
                        .long("kind")
                        .help("`api` (default) or `schema` — classifies the uploaded document."),
                )
                .arg(
                    Arg::new("ns")
                        .long("ns"),
                )
                .arg(
                    Arg::new("source")
                        .long("source"),
                )
                .arg(
                    Arg::new("spec-text")
                        .long("spec-text")
                        .help("Inline spec text (yaml/json/graphql SDL). One of specText | url required."),
                )
                .arg(
                    Arg::new("url")
                        .long("url")
                        .help("URL to fetch the spec from (server-side)."),
                )
                .arg(
                    Arg::new("version")
                        .long("version")
                        .help("Explicit version label; defaults to the spec's info.version."),
                )
                .arg(
                    Arg::new("dist-tag")
                        .long("dist-tag")
                        .help("Dist-tag to publish this version under (default `latest`). `latest` makes\nit the current/default version; any other tag (`canary`, `beta`, …) is a\nside-channel — added without moving `latest`/current off the stable one."),
                )
                .arg(
                    Arg::new("project-id")
                        .long("project-id")
                        .help("The project the API belongs to. Set server-side by the platform gateway\nfrom the caller's current project; defaults to the seeded project."),
                )
                .subcommand(
                    Command::new("dist-tag")
                        .about("Proxy: create or move a dist-tag (`latest`, `canary`, …) to a version.")
                        .arg(
                            Arg::new("api-id")
                                .required(true),
                        )
                        .arg(
                            Arg::new("tag")
                                .long("tag")
                                .required(true),
                        )
                        .arg(
                            Arg::new("version")
                                .long("version")
                                .required(true),
                        ),
                ),
        )
        .subcommand(
            Command::new("auth")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(
                    Command::new("login")
                        .arg(
                            Arg::new("email")
                                .long("email")
                                .required(true),
                        )
                        .arg(
                            Arg::new("password")
                                .long("password")
                                .required(true),
                        ),
                )
                .subcommand(Command::new("logout"))
                .subcommand(
                    Command::new("register")
                        .arg(
                            Arg::new("email")
                                .long("email")
                                .required(true),
                        )
                        .arg(
                            Arg::new("password")
                                .long("password")
                                .required(true),
                        )
                        .arg(
                            Arg::new("name")
                                .long("name"),
                        ),
                ),
        )
        .subcommand(
            Command::new("context")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(
                    Command::new("select")
                        .about("Switch the caller's current project (persisted per-user).")
                        .arg(
                            Arg::new("project-id")
                                .long("project-id")
                                .required(true),
                        ),
                ),
        )
        .subcommand(
            Command::new("notifications")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(
                    Command::new("read")
                        .arg(
                            Arg::new("ids")
                                .long("ids")
                                .action(clap::ArgAction::Append)
                                .value_delimiter(','),
                        )
                        .arg(
                            Arg::new("all")
                                .long("all")
                                .num_args(0..=1)
                                .require_equals(true)
                                .default_missing_value("true")
                                .value_parser(clap::value_parser!(bool)),
                        ),
                ),
        )
        .subcommand(
            Command::new("releases")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(
                    Command::new("prepare")
                        .arg(
                            Arg::new("connection-id")
                                .long("connection-id")
                                .required(true),
                        )
                        .arg(
                            Arg::new("version-override")
                                .long("version-override"),
                        ),
                ),
        )
        .subcommand(
            Command::new("repo-connection")
                .subcommand_negates_reqs(true)
                .arg(
                    Arg::new("provider-id")
                        .long("provider-id")
                        .required(true),
                )
                .arg(
                    Arg::new("target-kind")
                        .long("target-kind")
                        .required(true),
                )
                .arg(
                    Arg::new("target-id")
                        .long("target-id")
                        .required(true),
                )
                .arg(
                    Arg::new("ref")
                        .long("ref"),
                )
                .arg(
                    Arg::new("repo")
                        .long("repo")
                        .help("For an existing repo: \"owner/name\". For a new repo: just the name.")
                        .required(true),
                )
                .arg(
                    Arg::new("create-repo")
                        .long("create-repo")
                        .num_args(0..=1)
                        .require_equals(true)
                        .default_missing_value("true")
                        .value_parser(clap::value_parser!(bool))
                        .help("When true, create `repo` (a name) under the account before connecting."),
                )
                .arg(
                    Arg::new("make-private")
                        .long("make-private")
                        .num_args(0..=1)
                        .require_equals(true)
                        .default_missing_value("true")
                        .value_parser(clap::value_parser!(bool))
                        .help("Visibility for a newly-created repo."),
                )
                .arg(
                    Arg::new("branch")
                        .long("branch"),
                )
                .arg(
                    Arg::new("prefix")
                        .long("prefix"),
                )
                .arg(
                    Arg::new("release-mode")
                        .long("release-mode"),
                )
                .arg(
                    Arg::new("auto-release")
                        .long("auto-release")
                        .num_args(0..=1)
                        .require_equals(true)
                        .default_missing_value("true")
                        .value_parser(clap::value_parser!(bool)),
                )
                .arg(
                    Arg::new("base-branch")
                        .long("base-branch"),
                )
                .arg(
                    Arg::new("dist-tags")
                        .long("dist-tags")
                        .help("Comma-separated dist-tags to react to (default `latest`; `*` = all)."),
                )
                .subcommand(
                    Command::new("release-config")
                        .about("Switch a connection between direct-push and PR-based release mode.")
                        .arg(
                            Arg::new("id")
                                .required(true),
                        )
                        .arg(
                            Arg::new("release-mode")
                                .long("release-mode")
                                .required(true),
                        )
                        .arg(
                            Arg::new("auto-release")
                                .long("auto-release")
                                .num_args(0..=1)
                                .require_equals(true)
                                .default_missing_value("true")
                                .value_parser(clap::value_parser!(bool))
                                .required(true),
                        )
                        .arg(
                            Arg::new("base-branch")
                                .long("base-branch"),
                        )
                        .arg(
                            Arg::new("prerelease")
                                .long("prerelease")
                                .num_args(0..=1)
                                .require_equals(true)
                                .default_missing_value("true")
                                .value_parser(clap::value_parser!(bool)),
                        )
                        .arg(
                            Arg::new("dist-tags")
                                .long("dist-tags")
                                .help("Comma-separated dist-tags this connection reacts to (default `latest`);\n`*` = all. Only a version published under a matching tag triggers a\nrelease."),
                        ),
                )
                .subcommand(
                    Command::new("sync")
                        .arg(
                            Arg::new("id")
                                .required(true),
                        ),
                ),
        )
        .subcommand(
            Command::new("sdk")
                .subcommand_negates_reqs(true)
                .arg(
                    Arg::new("api-id")
                        .long("api-id")
                        .required(true),
                )
                .arg(
                    Arg::new("name")
                        .long("name"),
                )
                .arg(
                    Arg::new("id")
                        .long("id")
                        .help("Explicit id/slug for the SDK. Slugified server-side; defaults to a slug of\n`name`. Lets the display title (`name`) be decoupled from the stable id."),
                )
                .arg(
                    Arg::new("description")
                        .long("description"),
                )
                .arg(
                    Arg::new("ns")
                        .long("ns"),
                )
                .subcommand(
                    Command::new("build")
                        .arg(
                            Arg::new("sdk-id")
                                .required(true),
                        )
                        .arg(
                            Arg::new("api-version")
                                .long("api-version")
                                .help("API version to build every target from; defaults to the API's current."),
                        ),
                )
                .subcommand(
                    Command::new("target")
                        .arg(
                            Arg::new("sdk-id")
                                .required(true),
                        )
                        .arg(
                            Arg::new("language")
                                .long("language")
                                .required(true),
                        )
                        .arg(
                            Arg::new("package-name")
                                .long("package-name"),
                        )
                        .arg(
                            Arg::new("version")
                                .long("version")
                                .help("API version to generate from; defaults to the API's current version."),
                        )
                        .arg(
                            Arg::new("sdk-json")
                                .long("sdk-json")
                                .help("A full custom sdk.json (behavior + per-language emitter options), serialized.\nWhen set it's applied over the derived defaults during generation (the\n\"wizard\" flow); empty → the auto-derived config."),
                        ),
                )
                .subcommand(
                    Command::new("version")
                        .about("Create a new SDK version: bump + rebuild every target, recorded as a build.")
                        .arg(
                            Arg::new("sdk-id")
                                .required(true),
                        )
                        .arg(
                            Arg::new("version")
                                .long("version")
                                .help("The new SDK version (e.g. \"0.2.0\").")
                                .required(true),
                        )
                        .arg(
                            Arg::new("api-version")
                                .long("api-version")
                                .help("API version to build from; defaults to the API's current."),
                        ),
                ),
        )
        .subcommand(
            Command::new("api-key")
                .arg(
                    Arg::new("name")
                        .long("name")
                        .required(true),
                ),
        )
        .subcommand(
            Command::new("docs-project")
                .arg(
                    Arg::new("api-id")
                        .long("api-id")
                        .required(true),
                )
                .arg(
                    Arg::new("name")
                        .long("name")
                        .required(true),
                )
                .arg(
                    Arg::new("theme")
                        .long("theme"),
                ),
        )
        .subcommand(
            Command::new("git-provider")
                .arg(
                    Arg::new("kind")
                        .long("kind")
                        .required(true),
                )
                .arg(
                    Arg::new("name")
                        .long("name"),
                )
                .arg(
                    Arg::new("base-url")
                        .long("base-url")
                        .help("Provider base URL; optional for github.com/gitlab.com, required for self-hosted."),
                )
                .arg(
                    Arg::new("token")
                        .long("token")
                        .required(true),
                ),
        )
        .subcommand(
            Command::new("mcp-server")
                .arg(
                    Arg::new("api-id")
                        .long("api-id")
                        .required(true),
                )
                .arg(
                    Arg::new("name")
                        .long("name")
                        .required(true),
                )
                .arg(
                    Arg::new("transport")
                        .long("transport"),
                )
                .arg(
                    Arg::new("url")
                        .long("url"),
                ),
        )
        .subcommand(
            Command::new("member")
                .arg(
                    Arg::new("email")
                        .long("email")
                        .required(true),
                )
                .arg(
                    Arg::new("role")
                        .long("role"),
                ),
        )
        .subcommand(
            Command::new("package-registry")
                .arg(
                    Arg::new("kind")
                        .long("kind")
                        .required(true),
                )
                .arg(
                    Arg::new("name")
                        .long("name"),
                )
                .arg(
                    Arg::new("url")
                        .long("url")
                        .help("Registry URL, or a local file-feed path.")
                        .required(true),
                )
                .arg(
                    Arg::new("token")
                        .long("token")
                        .help("Auth token; may be empty for an anonymous/local registry."),
                ),
        )
        .subcommand(
            Command::new("project")
                .arg(
                    Arg::new("name")
                        .long("name")
                        .required(true),
                ),
        )
        .subcommand(
            Command::new("registry-connection")
                .arg(
                    Arg::new("registry-id")
                        .long("registry-id")
                        .required(true),
                )
                .arg(
                    Arg::new("target-id")
                        .long("target-id")
                        .required(true),
                )
                .arg(
                    Arg::new("package-name")
                        .long("package-name")
                        .help("Override the published package name (defaults to the SDK's own name)."),
                )
                .arg(
                    Arg::new("auto-publish")
                        .long("auto-publish")
                        .num_args(0..=1)
                        .require_equals(true)
                        .default_missing_value("true")
                        .value_parser(clap::value_parser!(bool)),
                ),
        )
}

pub async fn run<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        ["create", "api"] => handle_create_api(ctx, o, cmd_path, m).await,
        ["create", "api", "dist-tag"] => handle_create_api_dist_tag(ctx, o, cmd_path, m).await,
        ["create", "auth", "login"] => handle_create_auth_login(ctx, o, cmd_path, m).await,
        ["create", "auth", "logout"] => handle_create_auth_logout(ctx, o, cmd_path, m).await,
        ["create", "auth", "register"] => handle_create_auth_register(ctx, o, cmd_path, m).await,
        ["create", "context", "select"] => handle_create_context_select(ctx, o, cmd_path, m).await,
        ["create", "notifications", "read"] => {
            handle_create_notifications_read(ctx, o, cmd_path, m).await
        }
        ["create", "releases", "prepare"] => {
            handle_create_releases_prepare(ctx, o, cmd_path, m).await
        }
        ["create", "repo-connection"] => handle_create_repo_connection(ctx, o, cmd_path, m).await,
        ["create", "repo-connection", "release-config"] => {
            handle_create_repo_connection_release_config(ctx, o, cmd_path, m).await
        }
        ["create", "repo-connection", "sync"] => {
            handle_create_repo_connection_sync(ctx, o, cmd_path, m).await
        }
        ["create", "sdk"] => handle_create_sdk(ctx, o, cmd_path, m).await,
        ["create", "sdk", "build"] => handle_create_sdk_build(ctx, o, cmd_path, m).await,
        ["create", "sdk", "target"] => handle_create_sdk_target(ctx, o, cmd_path, m).await,
        ["create", "sdk", "version"] => handle_create_sdk_version(ctx, o, cmd_path, m).await,
        ["create", "api-key"] => handle_create_api_key(ctx, o, cmd_path, m).await,
        ["create", "docs-project"] => handle_create_docs_project(ctx, o, cmd_path, m).await,
        ["create", "git-provider"] => handle_create_git_provider(ctx, o, cmd_path, m).await,
        ["create", "mcp-server"] => handle_create_mcp_server(ctx, o, cmd_path, m).await,
        ["create", "member"] => handle_create_member(ctx, o, cmd_path, m).await,
        ["create", "package-registry"] => handle_create_package_registry(ctx, o, cmd_path, m).await,
        ["create", "project"] => handle_create_project(ctx, o, cmd_path, m).await,
        ["create", "registry-connection"] => {
            handle_create_registry_connection(ctx, o, cmd_path, m).await
        }
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

async fn handle_create_api<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/apis".to_string();
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("name") {
        body.insert("name".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("id") {
        body.insert("id".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("format") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("format".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("kind") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("kind".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("ns") {
        body.insert("ns".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("source") {
        body.insert("source".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("spec-text") {
        body.insert("specText".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("url") {
        body.insert("url".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("version") {
        body.insert("version".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("dist-tag") {
        body.insert("distTag".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("project-id") {
        body.insert(
            "projectId".to_string(),
            serde_json::Value::String(v.clone()),
        );
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_create_api_dist_tag<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let api_id = m
        .get_one::<String>("api-id")
        .map(String::as_str)
        .unwrap_or("");
    let path = format!("/apis/{}/dist-tags", runtime::path_escape(api_id));
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("tag") {
        body.insert("tag".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("version") {
        body.insert("version".to_string(), serde_json::Value::String(v.clone()));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_create_auth_login<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/auth/login".to_string();
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("email") {
        body.insert("email".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("password") {
        body.insert("password".to_string(), serde_json::Value::String(v.clone()));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_create_auth_logout<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    _m: &ArgMatches,
) -> ExitCode {
    let path = "/auth/logout".to_string();
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_create_auth_register<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/auth/register".to_string();
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("email") {
        body.insert("email".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("password") {
        body.insert("password".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("name") {
        body.insert("name".to_string(), serde_json::Value::String(v.clone()));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_create_context_select<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/context/select".to_string();
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("project-id") {
        body.insert(
            "projectId".to_string(),
            serde_json::Value::String(v.clone()),
        );
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_create_notifications_read<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/notifications/read".to_string();
    let mut body = serde_json::Map::new();
    if let Some(vs) = m.get_many::<String>("ids") {
        body.insert(
            "ids".to_string(),
            serde_json::Value::Array(vs.cloned().map(serde_json::Value::String).collect()),
        );
    }
    if let Some(v) = m.get_one::<bool>("all") {
        body.insert("all".to_string(), serde_json::Value::Bool(*v));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_create_releases_prepare<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/releases/prepare".to_string();
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("connection-id") {
        body.insert(
            "connectionId".to_string(),
            serde_json::Value::String(v.clone()),
        );
    }
    if let Some(v) = m.get_one::<String>("version-override") {
        body.insert(
            "versionOverride".to_string(),
            serde_json::Value::String(v.clone()),
        );
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_create_repo_connection<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/repo-connections".to_string();
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("provider-id") {
        body.insert(
            "providerId".to_string(),
            serde_json::Value::String(v.clone()),
        );
    }
    if let Some(v) = m.get_one::<String>("target-kind") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("targetKind".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("target-id") {
        body.insert("targetId".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("ref") {
        body.insert("ref".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("repo") {
        body.insert("repo".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<bool>("create-repo") {
        body.insert("createRepo".to_string(), serde_json::Value::Bool(*v));
    }
    if let Some(v) = m.get_one::<bool>("make-private") {
        body.insert("makePrivate".to_string(), serde_json::Value::Bool(*v));
    }
    if let Some(v) = m.get_one::<String>("branch") {
        body.insert("branch".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("prefix") {
        body.insert("prefix".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("release-mode") {
        body.insert(
            "releaseMode".to_string(),
            serde_json::Value::String(v.clone()),
        );
    }
    if let Some(v) = m.get_one::<bool>("auto-release") {
        body.insert("autoRelease".to_string(), serde_json::Value::Bool(*v));
    }
    if let Some(v) = m.get_one::<String>("base-branch") {
        body.insert(
            "baseBranch".to_string(),
            serde_json::Value::String(v.clone()),
        );
    }
    if let Some(v) = m.get_one::<String>("dist-tags") {
        body.insert("distTags".to_string(), serde_json::Value::String(v.clone()));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_create_repo_connection_release_config<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let id = m.get_one::<String>("id").map(String::as_str).unwrap_or("");
    let path = format!(
        "/repo-connections/{}/release-config",
        runtime::path_escape(id)
    );
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("release-mode") {
        body.insert(
            "releaseMode".to_string(),
            serde_json::Value::String(v.clone()),
        );
    }
    if let Some(v) = m.get_one::<bool>("auto-release") {
        body.insert("autoRelease".to_string(), serde_json::Value::Bool(*v));
    }
    if let Some(v) = m.get_one::<String>("base-branch") {
        body.insert(
            "baseBranch".to_string(),
            serde_json::Value::String(v.clone()),
        );
    }
    if let Some(v) = m.get_one::<bool>("prerelease") {
        body.insert("prerelease".to_string(), serde_json::Value::Bool(*v));
    }
    if let Some(v) = m.get_one::<String>("dist-tags") {
        body.insert("distTags".to_string(), serde_json::Value::String(v.clone()));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_create_repo_connection_sync<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let id = m.get_one::<String>("id").map(String::as_str).unwrap_or("");
    let path = format!("/repo-connections/{}/sync", runtime::path_escape(id));
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_create_sdk<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/sdks".to_string();
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("api-id") {
        body.insert("apiId".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("name") {
        body.insert("name".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("id") {
        body.insert("id".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("description") {
        body.insert(
            "description".to_string(),
            serde_json::Value::String(v.clone()),
        );
    }
    if let Some(v) = m.get_one::<String>("ns") {
        body.insert("ns".to_string(), serde_json::Value::String(v.clone()));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_create_sdk_build<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let sdk_id = m
        .get_one::<String>("sdk-id")
        .map(String::as_str)
        .unwrap_or("");
    let path = format!("/sdks/{}/build", runtime::path_escape(sdk_id));
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("api-version") {
        body.insert(
            "apiVersion".to_string(),
            serde_json::Value::String(v.clone()),
        );
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_create_sdk_target<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let sdk_id = m
        .get_one::<String>("sdk-id")
        .map(String::as_str)
        .unwrap_or("");
    let path = format!("/sdks/{}/targets", runtime::path_escape(sdk_id));
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("language") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("language".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("package-name") {
        body.insert(
            "packageName".to_string(),
            serde_json::Value::String(v.clone()),
        );
    }
    if let Some(v) = m.get_one::<String>("version") {
        body.insert("version".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("sdk-json") {
        body.insert("sdkJson".to_string(), serde_json::Value::String(v.clone()));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_create_sdk_version<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let sdk_id = m
        .get_one::<String>("sdk-id")
        .map(String::as_str)
        .unwrap_or("");
    let path = format!("/sdks/{}/versions", runtime::path_escape(sdk_id));
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("version") {
        body.insert("version".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("api-version") {
        body.insert(
            "apiVersion".to_string(),
            serde_json::Value::String(v.clone()),
        );
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_create_api_key<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/api-keys".to_string();
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("name") {
        body.insert("name".to_string(), serde_json::Value::String(v.clone()));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_create_docs_project<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/docs-projects".to_string();
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("api-id") {
        body.insert("apiId".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("name") {
        body.insert("name".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("theme") {
        body.insert("theme".to_string(), serde_json::Value::String(v.clone()));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_create_git_provider<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/git-providers".to_string();
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("kind") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("kind".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("name") {
        body.insert("name".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("base-url") {
        body.insert("baseUrl".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("token") {
        body.insert("token".to_string(), serde_json::Value::String(v.clone()));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_create_mcp_server<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/mcp-servers".to_string();
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("api-id") {
        body.insert("apiId".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("name") {
        body.insert("name".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("transport") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("transport".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("url") {
        body.insert("url".to_string(), serde_json::Value::String(v.clone()));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_create_member<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/members".to_string();
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("email") {
        body.insert("email".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("role") {
        body.insert("role".to_string(), serde_json::Value::String(v.clone()));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_create_package_registry<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/package-registries".to_string();
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("kind") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("kind".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("name") {
        body.insert("name".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("url") {
        body.insert("url".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("token") {
        body.insert("token".to_string(), serde_json::Value::String(v.clone()));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_create_project<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/projects".to_string();
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("name") {
        body.insert("name".to_string(), serde_json::Value::String(v.clone()));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_create_registry_connection<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/registry-connections".to_string();
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("registry-id") {
        body.insert(
            "registryId".to_string(),
            serde_json::Value::String(v.clone()),
        );
    }
    if let Some(v) = m.get_one::<String>("target-id") {
        body.insert("targetId".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("package-name") {
        body.insert(
            "packageName".to_string(),
            serde_json::Value::String(v.clone()),
        );
    }
    if let Some(v) = m.get_one::<bool>("auto-publish") {
        body.insert("autoPublish".to_string(), serde_json::Value::Bool(*v));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}
