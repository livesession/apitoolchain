// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::gen::runtime::{self, CliOverrides, Context};

pub fn command() -> Command {
    Command::new("get")
        .subcommand_required(true)
        .arg_required_else_help(true)
        .subcommand(
            Command::new("auth")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(Command::new("me")),
        )
        .subcommand(
            Command::new("git-provider")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(
                    Command::new("branches")
                        .about("Branch names of one repo (for the branch picker after a repo is chosen).")
                        .arg(
                            Arg::new("id")
                                .required(true),
                        )
                        .arg(
                            Arg::new("repo")
                                .long("repo")
                                .required(true),
                        ),
                )
                .subcommand(
                    Command::new("repos")
                        .about("Repos the provider's token can access (for the connect picker).")
                        .arg(
                            Arg::new("id")
                                .required(true),
                        ),
                ),
        )
        .subcommand(
            Command::new("overview")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(Command::new("stats")),
        )
        .subcommand(
            Command::new("sdk")
                .visible_alias("sdks")
                .subcommand_negates_reqs(true)
                .arg(Arg::new("id"))
                .arg(
                    Arg::new("api-id")
                        .long("api-id"),
                )
                .subcommand(
                    Command::new("targets")
                        .arg(
                            Arg::new("sdk-id")
                                .required(true),
                        ),
                )
                .subcommand(
                    Command::new("versions")
                        .about("SDK version/build history (newest first).")
                        .arg(
                            Arg::new("sdk-id")
                                .required(true),
                        ),
                ),
        )
        .subcommand(
            Command::new("api")
                .visible_alias("apis")
                .arg(Arg::new("api-id")),
        )
        .subcommand(Command::new("api-keys"))
        .subcommand(Command::new("context"))
        .subcommand(
            Command::new("docs-projects")
                .arg(
                    Arg::new("api-id")
                        .long("api-id"),
                ),
        )
        .subcommand(Command::new("git-providers"))
        .subcommand(
            Command::new("mcp-servers")
                .arg(
                    Arg::new("api-id")
                        .long("api-id"),
                ),
        )
        .subcommand(Command::new("members"))
        .subcommand(Command::new("notifications"))
        .subcommand(Command::new("package-registries"))
        .subcommand(Command::new("projects"))
        .subcommand(
            Command::new("registry-connections")
                .arg(
                    Arg::new("target-id")
                        .long("target-id"),
                ),
        )
        .subcommand(
            Command::new("release")
                .visible_alias("releases")
                .arg(Arg::new("id"))
                .arg(
                    Arg::new("connection-id")
                        .long("connection-id"),
                ),
        )
        .subcommand(
            Command::new("repo-connections")
                .arg(
                    Arg::new("target-kind")
                        .long("target-kind"),
                )
                .arg(
                    Arg::new("target-id")
                        .long("target-id"),
                ),
        )
        .subcommand(
            Command::new("sdk-target")
                .visible_alias("sdk-targets")
                .arg(Arg::new("id"))
                .arg(
                    Arg::new("api-id")
                        .long("api-id"),
                ),
        )
        .subcommand(
            Command::new("usage")
                .arg(
                    Arg::new("range")
                        .long("range")
                        .required(true),
                ),
        )
}

pub async fn run<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        ["get", "auth", "me"] => handle_get_auth_me(ctx, o, cmd_path, m).await,
        ["get", "git-provider", "branches"] => {
            handle_get_git_provider_branches(ctx, o, cmd_path, m).await
        }
        ["get", "git-provider", "repos"] => {
            handle_get_git_provider_repos(ctx, o, cmd_path, m).await
        }
        ["get", "overview", "stats"] => handle_get_overview_stats(ctx, o, cmd_path, m).await,
        ["get", "sdk"] => handle_get_sdk(ctx, o, cmd_path, m).await,
        ["get", "sdk", "targets"] => handle_get_sdk_targets(ctx, o, cmd_path, m).await,
        ["get", "sdk", "versions"] => handle_get_sdk_versions(ctx, o, cmd_path, m).await,
        ["get", "api"] => handle_get_api(ctx, o, cmd_path, m).await,
        ["get", "api-keys"] => handle_get_api_keys(ctx, o, cmd_path, m).await,
        ["get", "context"] => handle_get_context(ctx, o, cmd_path, m).await,
        ["get", "docs-projects"] => handle_get_docs_projects(ctx, o, cmd_path, m).await,
        ["get", "git-providers"] => handle_get_git_providers(ctx, o, cmd_path, m).await,
        ["get", "mcp-servers"] => handle_get_mcp_servers(ctx, o, cmd_path, m).await,
        ["get", "members"] => handle_get_members(ctx, o, cmd_path, m).await,
        ["get", "notifications"] => handle_get_notifications(ctx, o, cmd_path, m).await,
        ["get", "package-registries"] => handle_get_package_registries(ctx, o, cmd_path, m).await,
        ["get", "projects"] => handle_get_projects(ctx, o, cmd_path, m).await,
        ["get", "registry-connections"] => {
            handle_get_registry_connections(ctx, o, cmd_path, m).await
        }
        ["get", "release"] => handle_get_release(ctx, o, cmd_path, m).await,
        ["get", "repo-connections"] => handle_get_repo_connections(ctx, o, cmd_path, m).await,
        ["get", "sdk-target"] => handle_get_sdk_target(ctx, o, cmd_path, m).await,
        ["get", "usage"] => handle_get_usage(ctx, o, cmd_path, m).await,
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

async fn handle_get_auth_me<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    _m: &ArgMatches,
) -> ExitCode {
    let path = "/auth/me".to_string();
    let req = runtime::Request {
        method: "GET",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_get_git_provider_branches<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let id = m.get_one::<String>("id").map(String::as_str).unwrap_or("");
    let path = format!("/git-providers/{}/branches", runtime::path_escape(id));
    let mut query: Vec<(&'static str, String)> = Vec::new();
    if let Some(v) = m.get_one::<String>("repo") {
        query.push(("repo", v.clone()));
    }
    let req = runtime::Request {
        method: "GET",
        path,
        query,
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_get_git_provider_repos<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let id = m.get_one::<String>("id").map(String::as_str).unwrap_or("");
    let path = format!("/git-providers/{}/repos", runtime::path_escape(id));
    let req = runtime::Request {
        method: "GET",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_get_overview_stats<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    _m: &ArgMatches,
) -> ExitCode {
    let path = "/overview/stats".to_string();
    let req = runtime::Request {
        method: "GET",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_get_sdk<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let id = m.get_one::<String>("id").map(String::as_str).unwrap_or("");
    let (method, path) = if id.is_empty() {
        ("GET", "/sdks".to_string())
    } else {
        ("GET", format!("/sdks/{}", runtime::path_escape(id)))
    };
    let mut query: Vec<(&'static str, String)> = Vec::new();
    if let Some(v) = m.get_one::<String>("api-id") {
        query.push(("apiId", v.clone()));
    }
    let req = runtime::Request {
        method: method,
        path,
        query,
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_get_sdk_targets<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let sdk_id = m
        .get_one::<String>("sdk-id")
        .map(String::as_str)
        .unwrap_or("");
    let path = format!("/sdks/{}/targets", runtime::path_escape(sdk_id));
    let req = runtime::Request {
        method: "GET",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_get_sdk_versions<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let sdk_id = m
        .get_one::<String>("sdk-id")
        .map(String::as_str)
        .unwrap_or("");
    let path = format!("/sdks/{}/versions", runtime::path_escape(sdk_id));
    let req = runtime::Request {
        method: "GET",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_get_api<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let api_id = m
        .get_one::<String>("api-id")
        .map(String::as_str)
        .unwrap_or("");
    let (method, path) = if api_id.is_empty() {
        ("GET", "/apis".to_string())
    } else {
        ("GET", format!("/apis/{}", runtime::path_escape(api_id)))
    };
    let req = runtime::Request {
        method: method,
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_get_api_keys<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    _m: &ArgMatches,
) -> ExitCode {
    let path = "/api-keys".to_string();
    let req = runtime::Request {
        method: "GET",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_get_context<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    _m: &ArgMatches,
) -> ExitCode {
    let path = "/context".to_string();
    let req = runtime::Request {
        method: "GET",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_get_docs_projects<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/docs-projects".to_string();
    let mut query: Vec<(&'static str, String)> = Vec::new();
    if let Some(v) = m.get_one::<String>("api-id") {
        query.push(("apiId", v.clone()));
    }
    let req = runtime::Request {
        method: "GET",
        path,
        query,
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_get_git_providers<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    _m: &ArgMatches,
) -> ExitCode {
    let path = "/git-providers".to_string();
    let req = runtime::Request {
        method: "GET",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_get_mcp_servers<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/mcp-servers".to_string();
    let mut query: Vec<(&'static str, String)> = Vec::new();
    if let Some(v) = m.get_one::<String>("api-id") {
        query.push(("apiId", v.clone()));
    }
    let req = runtime::Request {
        method: "GET",
        path,
        query,
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_get_members<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    _m: &ArgMatches,
) -> ExitCode {
    let path = "/members".to_string();
    let req = runtime::Request {
        method: "GET",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_get_notifications<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    _m: &ArgMatches,
) -> ExitCode {
    let path = "/notifications".to_string();
    let req = runtime::Request {
        method: "GET",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_get_package_registries<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    _m: &ArgMatches,
) -> ExitCode {
    let path = "/package-registries".to_string();
    let req = runtime::Request {
        method: "GET",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_get_projects<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    _m: &ArgMatches,
) -> ExitCode {
    let path = "/projects".to_string();
    let req = runtime::Request {
        method: "GET",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_get_registry_connections<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/registry-connections".to_string();
    let mut query: Vec<(&'static str, String)> = Vec::new();
    if let Some(v) = m.get_one::<String>("target-id") {
        query.push(("targetId", v.clone()));
    }
    let req = runtime::Request {
        method: "GET",
        path,
        query,
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_get_release<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let id = m.get_one::<String>("id").map(String::as_str).unwrap_or("");
    let (method, path) = if id.is_empty() {
        ("GET", "/releases".to_string())
    } else {
        ("GET", format!("/releases/{}", runtime::path_escape(id)))
    };
    let mut query: Vec<(&'static str, String)> = Vec::new();
    if let Some(v) = m.get_one::<String>("connection-id") {
        query.push(("connectionId", v.clone()));
    }
    let req = runtime::Request {
        method: method,
        path,
        query,
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_get_repo_connections<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/repo-connections".to_string();
    let mut query: Vec<(&'static str, String)> = Vec::new();
    if let Some(v) = m.get_one::<String>("target-kind") {
        query.push(("targetKind", v.clone()));
    }
    if let Some(v) = m.get_one::<String>("target-id") {
        query.push(("targetId", v.clone()));
    }
    let req = runtime::Request {
        method: "GET",
        path,
        query,
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_get_sdk_target<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let id = m.get_one::<String>("id").map(String::as_str).unwrap_or("");
    let (method, path) = if id.is_empty() {
        ("GET", "/sdk-targets".to_string())
    } else {
        ("GET", format!("/sdk-targets/{}", runtime::path_escape(id)))
    };
    let mut query: Vec<(&'static str, String)> = Vec::new();
    if let Some(v) = m.get_one::<String>("api-id") {
        query.push(("apiId", v.clone()));
    }
    let req = runtime::Request {
        method: method,
        path,
        query,
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_get_usage<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/usage".to_string();
    let mut query: Vec<(&'static str, String)> = Vec::new();
    if let Some(v) = m.get_one::<String>("range") {
        query.push(("range", v.clone()));
    }
    let req = runtime::Request {
        method: "GET",
        path,
        query,
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}
