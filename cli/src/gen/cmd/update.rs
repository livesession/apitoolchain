// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::gen::runtime::{self, CliOverrides, Context};

pub fn command() -> Command {
    Command::new("update")
        .subcommand_required(true)
        .arg_required_else_help(true)
        .subcommand(
            Command::new("member")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(
                    Command::new("role")
                        .arg(Arg::new("user-id").required(true))
                        .arg(Arg::new("role").long("role").required(true)),
                ),
        )
        .subcommand(
            Command::new("api")
                .about("Proxy: rename an entry (display name + description). id/ns immutable.")
                .arg(Arg::new("api-id").required(true))
                .arg(Arg::new("name").long("name"))
                .arg(Arg::new("description").long("description")),
        )
        .subcommand(
            Command::new("context")
                .arg(Arg::new("org-name").long("org-name"))
                .arg(Arg::new("project-name").long("project-name")),
        )
        .subcommand(
            Command::new("project")
                .arg(Arg::new("id").required(true))
                .arg(Arg::new("name").long("name").required(true)),
        )
}

pub async fn run<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        ["update", "member", "role"] => handle_update_member_role(ctx, o, cmd_path, m).await,
        ["update", "api"] => handle_update_api(ctx, o, cmd_path, m).await,
        ["update", "context"] => handle_update_context(ctx, o, cmd_path, m).await,
        ["update", "project"] => handle_update_project(ctx, o, cmd_path, m).await,
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

async fn handle_update_member_role<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let user_id = m
        .get_one::<String>("user-id")
        .map(String::as_str)
        .unwrap_or("");
    let path = format!("/members/{}/role", runtime::path_escape(user_id));
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("role") {
        body.insert("role".to_string(), serde_json::Value::String(v.clone()));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "PATCH",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_update_api<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let api_id = m
        .get_one::<String>("api-id")
        .map(String::as_str)
        .unwrap_or("");
    let path = format!("/apis/{}", runtime::path_escape(api_id));
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("name") {
        body.insert("name".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("description") {
        body.insert(
            "description".to_string(),
            serde_json::Value::String(v.clone()),
        );
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "PATCH",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_update_context<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/context".to_string();
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("org-name") {
        body.insert("orgName".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("project-name") {
        body.insert(
            "projectName".to_string(),
            serde_json::Value::String(v.clone()),
        );
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "PATCH",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_update_project<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let id = m.get_one::<String>("id").map(String::as_str).unwrap_or("");
    let path = format!("/projects/{}", runtime::path_escape(id));
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("name") {
        body.insert("name".to_string(), serde_json::Value::String(v.clone()));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "PATCH",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}
