[package]
name = "apitoolchain_uniform"
edition.workspace = true
version.workspace = true
license.workspace = true
publish = false

# The shared Uniform data model (S6+): serde structs mirroring
# packages/uniform/src/types.ts — the output format every converter crate
# (gql, openapi, …) emits — plus the JSON canonicalization helpers the
# fixture-parity tests compare with.

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
apitoolchain_parity = { path = "../apitoolchain_parity" }
